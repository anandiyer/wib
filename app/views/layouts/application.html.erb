<!DOCTYPE html>
<html>
<head>
  <title>Whoisbetter</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
    <div id="user_nav">
        <% if user_signed_in? %>
            Signed in as <%= current_user.email %> Not you?
            <%= link_to "Sign Out", destroy_user_session_path %>
        <% else %>
            <%= link_to "Sign In", authentications_path%>
        <% end %>
    </div>

<%= yield %>

</body>
</html>
